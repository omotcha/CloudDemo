server:
  port: 3377 #server port
spring:
  application:
    name: config-client #server name
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:1111
      config:
        server-addr: 127.0.0.1:1111
        file-extension: yaml